1...this problem can be solved by using heap------time--O(n2logk)--space--O(k)
   
      (largest element--use min heap
     smallest element--use max heap
       **and we will restrict the size of heap  to k so that 
        insert--O(logk) and delete--O(logk)
        and the size of heap will be--O(k))

2.second method by using binary search---time--O(nlog(maxval))--O(n)--space--O(1)