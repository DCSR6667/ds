1...merging--is the concept of combining  2 or more sorted arrays into a single sorted array---O(nlogn)
     merge sort is done by using merging concept 
      inorder to apply merging concept compulsary we need sorted arrays that why we split the given array until we reached to individual elements
        and then we will apply merging